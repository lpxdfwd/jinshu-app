name: jinshu_app
description: A new Flutter project.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2

  # socket.io
  adhara_socket_io: ^0.4.1

  flutter_socket_io: ^0.6.0

  # 请求
  http: ^0.12.0

  # 提示toast
  fluttertoast: ^3.1.3

  # 缓存文件
  path_provider: ^0.5.0+1

  #本地存储
  shared_preferences: ^0.5.4+5

  #这个主要是MD5  base64之类 转码
  crypto: ^2.0.6

  #这是一个预加载的透明图 ，如果需要用assert图或者是自定义可以不需要  这里为了方便
  transparent_image: ^0.1.0

  #网络图片缓存
  cached_network_image: ^1.1.3

  #轮播组件
  flutter_swiper: ^1.1.6

  #item活动删除插件
  flutter_slidable: ^0.5.4

  #倒计时插件
  common_utils: ^1.1.1

  #选择图片或拍照
  image_picker: ^0.6.0+8

  #图片剪裁
  image_crop: ^0.3.1

  #请求
  dio: ^3.0.9

  numberpicker: ^1.0.0

  date_format: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
   - images/default-pic.png
   - images/tab-me.png
   - images/tab-jinshu-active.png
   - images/tab-jinshu.png
   - images/tab-settings.png
   - images/tab-settings-active.png
   - images/tab-follow.png
   - images/tab-follow-active.png
   - images/tab-jinshu-icon.png
   - images/search-icon.png
   - images/swiper-1.png
   - images/swiper-2.png
   - images/start-bg.png
   - images/swiper-btn.png
   - images/swiper-confirm-btn.png
   - images/login-bg.png
   - images/mobile-login-icon.png
   - images/right-arrow-icon.png
   - images/login-top-bg.png
   - images/close-icon.png
   - images/register-bg.png
   - images/sex-man-icon.png
   - images/sex-woman-icon.png
   - images/select-sex.png
   - images/logo.png
   - images/top-mask.png
   - images/root-head-bg.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: iconfont
      fonts:
        - asset: fonts/iconfont.ttf
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
